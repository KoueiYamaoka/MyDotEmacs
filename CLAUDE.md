# CLAUDE.md
This is a personal Emacs configuration (`~/.emacs.d`) for a Japanese-language user running Emacs on Linux and macOS. All configuration lives in `init.el`, managed via the `leaf` package manager.

